{
  "name": "document-appraisal-ai-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
